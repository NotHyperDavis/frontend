info:
  name: "27"
  type: http

http:
  method: GET
  url: https://pokeapi.co/api/v2/ability/chlorophyll
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
