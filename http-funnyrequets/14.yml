info:
  name: "14"
  type: http

http:
  method: GET
  url: https://rickandmortyapi.com/api/character?gender=Female&status=Alive
  params:
    - name: gender
      value: Female
      type: query
    - name: status
      value: Alive
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
