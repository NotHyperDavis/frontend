info:
  name: "28"
  type: http

http:
  method: GET
  url: https://pokeapi.co/api/v2/ability/battle-armor
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
