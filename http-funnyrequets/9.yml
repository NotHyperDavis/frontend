info:
  name: "9"
  type: http

http:
  method: PATCH
  url: https://jsonplaceholder.typicode.com/posts/1
  body:
    type: json
    data: |-
      {
        "title": "Apenas o título foi alterado!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
