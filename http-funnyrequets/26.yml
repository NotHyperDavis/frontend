info:
  name: "26"
  type: http

http:
  method: GET
  url: https://pokeapi.co/api/v2/pokemon/?ability=chlorophyll
  params:
    - name: ability
      value: chlorophyll
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
