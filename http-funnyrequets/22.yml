info:
  name: "22"
  type: http

http:
  method: GET
  url: https://pokeapi.co/api/v2/pokemon/ditto
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
