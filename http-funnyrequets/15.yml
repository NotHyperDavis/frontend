info:
  name: "15"
  type: http

http:
  method: GET
  url: https://rickandmortyapi.com/api/character?gender=Female&status=Alive&page=2
  params:
    - name: gender
      value: Female
      type: query
    - name: status
      value: Alive
      type: query
    - name: page
      value: "2"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
