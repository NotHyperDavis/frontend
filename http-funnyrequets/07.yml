info:
  name: "07"
  type: http

http:
  method: POST
  url: https://jsonplaceholder.typicode.com/posts
  body:
    type: json
    data: |
      {
        "title": "Post Inválido",
        "body": "Isto vai dar erro"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
