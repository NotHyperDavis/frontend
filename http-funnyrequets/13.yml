info:
  name: "13"
  type: http

http:
  method: GET
  url: https://rickandmortyapi.com/api/character/?name=Rick&status=Dead
  params:
    - name: name
      value: Rick
      type: query
    - name: status
      value: Dead
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
