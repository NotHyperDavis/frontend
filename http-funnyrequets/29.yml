info:
  name: "29"
  type: http

http:
  method: GET
  url: https://pokeapi.co/api/v2/pokemon-species/25/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
