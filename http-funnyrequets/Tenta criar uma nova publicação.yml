info:
  name: Tenta criar uma nova publicação
  type: http

http:
  method: POST
  url: https://jsonplaceholder.typicode.com/posts
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
