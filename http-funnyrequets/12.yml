info:
  name: "12"
  type: http

http:
  method: GET
  url: https://rickandmortyapi.com/api/character/?name=Rick
  params:
    - name: name
      value: Rick
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
